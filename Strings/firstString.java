//package strings;

import java.util.*;
public class firstString {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String name = sc.nextLine();
        System.out.println(name);

        //to find the length of string we use str.length() . 
        //we have to put parenthesis here because in string it is a function wereas in array it was a property.
        System.out.println(name.length()); 
    }
}
